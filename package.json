{
	"name": "make-a-poll",
	"version": "1.0.0",
	"description": "",
	"private": true,
	"scripts": {
		"dev:client": "npm run dev --workspace=client",
		"dev:server": "npm run dev --workspace=server",
		"start": "npm run start --workspace=server",
		"build": "npm run build --workspace=client && npm run build --workspace=server",
		"dev": "npm run build --workspace=client && npm run dev:server"
	},
	"engines": {
		"npm": "7.20.6",
		"node": "14.15.3"
	},
	"workspaces": [
		"client",
		"server"
	],
	"keywords": [
		"socket",
		"react"
	],
	"license": "MIT",
	"dependencies": {
		"@types/node": "^12.0.0",
		"@types/uuid": "^8.3.3",
		"typescript": "^4.4.4",
		"uuid": "^8.3.2"
	},
	"devDependencies": {
		"cross-env": "^7.0.3"
	}
}
